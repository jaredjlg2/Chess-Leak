{
  "name": "chess-leak-android-web",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/prepare-assets.mjs",
    "build": "vite build",
    "prebuild:pages": "node scripts/prepare-assets.mjs",
    "build:pages": "vite build --mode pages"
  },
  "dependencies": {
    "chess.js": "1.4.0",
    "stockfish": "18.0.8"
  },
  "devDependencies": {
    "vite": "8.1.0"
  }
}
